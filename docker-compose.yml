version: '3.8'

services:
  front:
    build:
      context: .
      args:
        API_URL: "http://localhost:3000"
    command: "yarn run dev"
    volumes:
      - .:/app
    ports:
      - "$FRONT_PORT:3000"